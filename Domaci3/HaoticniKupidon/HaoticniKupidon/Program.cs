using System;
using System.Collections.Generic;
using System.Linq;
using System.ServiceModel;
using System.Web;

namespace HaoticniKupidon
{
    public class Program
    {
        public static void Main(string[] args)
        {
            //ServiceHost serviceHost = new ServiceHost(typeof(HaoticniKupidon));
            //serviceHost.AddServiceEndpoint(typeof(IHaoticniKupidon), new BasicHttpBinding(), "http://localhost:4000/Kupidon");
        }
    }
}